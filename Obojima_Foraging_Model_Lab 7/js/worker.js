importScripts('data.js','engine.js');
self.onmessage=e=>{const {trials=5000,dc=18,degreeOfSuccess=5,overrides={}}=e.data||{};const eng=ObojimaLabEngine.createEngine(overrides);const scenarios=[];let done=0;const total=eng.regions.reduce((s,r)=>s+r.search_areas.length,0)*trials;
 for(const r of eng.regions){for(const area of r.search_areas){const counts={},ref=[0,0,0,0,0,0],rarity={},fit={direct:0,related:0,none:0},geo={};let finds=0;for(let t=0;t<trials;t++){for(const x of eng.runHaul({region:r.name,area,dc,degreeOfSuccess})){counts[x.name]=(counts[x.name]||0)+1;ref[Math.round(x.refinementRelationship.ingredientValue)]++;rarity[x.rarity]=(rarity[x.rarity]||0)+1;fit[x.habitatRelationship]++;geo[x.regionRelationship]=(geo[x.regionRelationship]||0)+1;finds++}done++;if(t%1000===0)postMessage({type:'progress',done,total})}const avg=ref.reduce((s,n,i)=>s+n*i,0)/(finds||1);scenarios.push({region:r.name,area,civilization:+(eng.areas.find(a=>a.name===area)||{}).civilization||1,trials,finds,averageRefinement:avg,counts,refinementCounts:ref,rarityCounts:rarity,fitCounts:fit,geographyCounts:geo})}}
 postMessage({type:'complete',result:{trials,dc,degreeOfSuccess,scenarios,ingredients:eng.ingredients.map(x=>({name:x.name,rarity:x.rarity,refinement:x.refinement,forageable:x.forageable,areas:x.associated_search_areas,regions:x.regions}))}})};
